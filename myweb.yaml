---
apiVersion: v1
kind: Deployment
metadata:
namespace: jenkins
  labels:
    app: myweb
  name: myweb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myweb
  template:
    metadata:
      labels:
        app: myweb
    spec:
      serviceAccountName: jenkins
      containers:
      - image: tiestovarn/docker:latest
        imagePullPolicy: Always
        name: myweb

---
apiVersion: v1
kind: Service
metadata:
namespace: jenkins
  labels:
    app: myweb
  name: myweb
spec:
  ports:
  - nodePort: 32223
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: myweb
  type: NodePort
